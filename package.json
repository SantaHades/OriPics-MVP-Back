{
  "name": "oripics-backend",
  "version": "1.0.0",
  "description": "OriPics MVP Backend (Python)",
  "scripts": {
    "start": ".venv/bin/uvicorn main:app --host 0.0.0.0 --port ${PORT:-7860} --reload",
    "install-deps": ".venv/bin/pip install -r requirements.txt"
  }
}
